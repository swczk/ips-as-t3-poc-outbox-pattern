meta {
  name: Teste 1 - Pedido 3
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/pedidos
  body: json
  auth: none
}

body:json {
  {
    "clienteId": "{{clienteId3}}",
    "valor": 30.00
  }
}

tests {
  test("pedido criado", function() {
    expect(res.status).to.equal(201);
    expect(res.body.estado).to.equal("CRIADO");
  });
}
