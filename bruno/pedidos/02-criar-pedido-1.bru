meta {
  name: Teste 1 - Pedido 1
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/pedidos
  body: json
  auth: none
}

body:json {
  {
    "clienteId": "{{clienteId1}}",
    "valor": 10.00
  }
}

tests {
  test("pedido criado", function() {
    expect(res.status).to.equal(201);
    expect(res.body.estado).to.equal("CRIADO");
    expect(res.body.id).to.be.a("string");
  });
}
